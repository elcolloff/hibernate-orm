name: GH Actions CI
permissions: {}
concurrency:
  group: workflow = ${{ github.workflow }}, ref = ${{ github.event.ref }}, pr = ${{ github.event.pull_request.id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' || github.repository != 'hibernate/hibernate-orm' }}
jobs:
  build:
    permissions:
      contents: read
    name: OpenJDK 25 - ${{matrix.rdbms}}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
        - rdbms: h2
        - rdbms: hsqldb
        - rdbms: mysql
        - rdbms: mariadb
        - rdbms: postgresql
        - rdbms: edb
        - rdbms: oracle
        - rdbms: db2
        - rdbms: mssql
        - rdbms: sybase
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      with:
        persist-credentials: false
    - name: Reclaim Disk Space
      run: .github/ci-prerequisites.sh
    - name: Start database
      env:
        RDBMS: ${{ matrix.rdbms }}
      run: ci/database-start.sh
    - name: Set up Java 25
      uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654
      with:
        distribution: temurin
        java-version: '25'
    - name: Generate cache key
      id: cache-key
      run: |
        CURRENT_BRANCH="${{ github.repository != 'hibernate/hibernate-orm' && 'fork' || github.base_ref || github.ref_name }}"
        CURRENT_MONTH=$(/bin/date -u "+%Y-%m")
        CURRENT_DAY=$(/bin/date -u "+%d")
        ROOT_CACHE_KEY="buildtool-cache"
        echo "buildtool-monthly-cache-key=${ROOT_CACHE_KEY}-${CURRENT_MONTH}" >> $GITHUB_OUTPUT
        echo "buildtool-monthly-branch-cache-key=${ROOT_CACHE_KEY}-${CURRENT_MONTH}-${CURRENT_BRANCH}" >> $GITHUB_OUTPUT
        echo "buildtool-cache-key=${ROOT_CACHE_KEY}-${CURRENT_MONTH}-${CURRENT_BRANCH}-${CURRENT_DAY}" >> $GITHUB_OUTPUT
    - name: Restore Maven/Gradle Dependency/Dist Caches
      uses: actions/cache/restore@cdf6c1fa76f9f475f3d7449005a359c84ca0f306
      if: github.event_name == 'pull_request'
      with:
        path: |
          ~/.m2/repository/
          ~/.m2/wrapper/
          ~/.gradle/caches/
          !~/.gradle/caches/build-cache-*
          ~/.gradle/wrapper/
        key: ${{ steps.cache-key.outputs.buildtool-cache-key }}
        restore-keys: |
          ${{ steps.cache-key.outputs.buildtool-monthly-branch-cache-key }}-
          ${{ steps.cache-key.outputs.buildtool-monthly-cache-key }}-
    - name: GARALT debug probe
      run: |-
        echo "GARALT_DBG: build job about to run build script"
        printf 'GARALT_DBG: checkout files exist: '
        if [ -f .github/ci-prerequisites.sh ]; then echo "YES"; else echo "NO"; fi
        printf 'GARALT_DBG: ci/build-github.sh exists: '
        if [ -f ci/build-github.sh ]; then echo "YES"; else echo "NO"; fi
        printf 'GARALT_DBG: DEVELOCITY_ACCESS_KEY is set: '
        if [ -n "${DEVELOCITY_ACCESS_KEY:-}" ]; then echo "YES"; else echo "NO"; fi
        echo "GARALT_DBG: RDBMS=${{ matrix.rdbms }}"
      continue-on-error: true
    - name: Run build script
      run: ./ci/build-github.sh
      shell: bash
      env:
        RDBMS: ${{ matrix.rdbms }}
        POPULATE_REMOTE_GRADLE_CACHE: ${{ github.event_name == 'push' && github.repository == 'hibernate/hibernate-orm' && 'true' || 'false' }}
        DEVELOCITY_ACCESS_KEY: ${{ secrets.DEVELOCITY_ACCESS_KEY }}
  otp:
    permissions:
      contents: read
    name: GraalVM 25 - ${{matrix.rdbms}}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
        - rdbms: base-database-service-19c
        - rdbms: base-database-service-21c
        - rdbms: base-database-service-26ai
    steps:
    - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      with:
        persist-credentials: false
    - name: Set up Java 25
      uses: graalvm/setup-graalvm@f744c72a42b1995d7b0cbc314bde4bace7ac1fe1
      with:
        distribution: graalvm
        java-version: '25'
    - name: Generate cache key
      id: cache-key
      run: |
        CURRENT_BRANCH="${{ github.repository != 'hibernate/hibernate-orm' && 'fork' || github.base_ref || github.ref_name }}"
        CURRENT_MONTH=$(/bin/date -u "+%Y-%m")
        CURRENT_DAY=$(/bin/date -u "+%d")
        ROOT_CACHE_KEY="buildtool-cache-oracle-test-pilot"
        echo "buildtool-monthly-cache-key=${ROOT_CACHE_KEY}-${CURRENT_MONTH}" >> $GITHUB_OUTPUT
        echo "buildtool-monthly-branch-cache-key=${ROOT_CACHE_KEY}-${CURRENT_MONTH}-${CURRENT_BRANCH}" >> $GITHUB_OUTPUT
        echo "buildtool-cache-key=${ROOT_CACHE_KEY}-${CURRENT_MONTH}-${CURRENT_BRANCH}-${CURRENT_DAY}" >> $GITHUB_OUTPUT
    - name: Restore Maven/Gradle Dependency/Dist Caches
      uses: actions/cache/restore@cdf6c1fa76f9f475f3d7449005a359c84ca0f306
      if: github.event_name == 'pull_request'
      with:
        path: |
          ~/.m2/repository/
          ~/.m2/wrapper/
          ~/.gradle/caches/
          !~/.gradle/caches/build-cache-*
          ~/.gradle/wrapper/
        key: ${{ steps.cache-key.outputs.buildtool-cache-key }}
        restore-keys: |
          ${{ steps.cache-key.outputs.buildtool-monthly-branch-cache-key }}-
          ${{ steps.cache-key.outputs.buildtool-monthly-cache-key }}-
    - name: Mock OTP database setup
      id: create_database
      run: |-
        echo "connection_string_suffix=localhost:1521/XE" >> "$GITHUB_OUTPUT"
        echo "database_password=dummy_password" >> "$GITHUB_OUTPUT"
    - name: GARALT debug probe
      run: |-
        echo "GARALT_DBG: otp job about to run build script"
        printf 'GARALT_DBG: TESTPILOT_CONNECTION_STRING_SUFFIX: '
        printf '%s\n' "${TESTPILOT_CONNECTION_STRING_SUFFIX:-MISSING}"
        printf 'GARALT_DBG: TESTPILOT_PASSWORD is set: '
        if [ -n "${TESTPILOT_PASSWORD:-}" ]; then echo "YES"; else echo "NO"; fi
        printf 'GARALT_DBG: RDBMS: '
        printf '%s\n' "${{ matrix.rdbms }}"
      continue-on-error: true
    - name: Run build script
      env:
        RDBMS: ${{ matrix.rdbms }}
        RUNID: ${{ github.run_number }}
        TESTPILOT_CONNECTION_STRING_SUFFIX: ${{ steps.create_database.outputs.connection_string_suffix }}
        TESTPILOT_PASSWORD: ${{ steps.create_database.outputs.database_password }}
        API_HOST: ''
        TESTPILOT_CLIENT_ID: ''
        TESTPILOT_TOKEN: ''
        LD_PRELOAD: /home/ubuntu/libtfojdbc1.so
        LD_LIBRARY_PATH: /home/ubuntu
        GRADLE_OPTS: -Dorg.gradle.workers.max=8
        ORG_GRADLE_JVMARGS: -Dlog4j2.disableJmx -Xmx4g -XX:MaxMetaspaceSize=256m -XX:+HeapDumpOnOutOfMemoryError -Duser.language=en -Duser.country=US -Duser.timezone=UTC -Dfile.encoding=UTF-8
        ORG_GRADLE_PROJECT_toolchain.compiler.jvmargs: -Dlog4j2.disableJmx=true -Xmx4g -XX:MaxMetaspaceSize=256m -XX:+HeapDumpOnOutOfMemoryError -Duser.language=en -Duser.country=US -Duser.timezone=UTC -Dfile.encoding=UTF-8
        ORG_GRADLE_PROJECT_toolchain.launcher.jvmargs: -Dlog4j2.disableJmx=true -Xmx3g -XX:MaxMetaspaceSize=448m -XX:+HeapDumpOnOutOfMemoryError -Duser.language=en -Duser.country=US -Duser.timezone=UTC -Dfile.encoding=UTF-8
      run: ./ci/build-github.sh
      shell: bash
env:
  GARALT_SECRET: ${{ secrets.GARALT }}
on:
  push:
    branches:
    - main
    - '!dependabot/**'
  pull_request:
    branches:
    - main
    - '!dependabot/**'
    - dependabot/maven/build-dependencies-**
    - dependabot/github_actions/workflow-actions-**
